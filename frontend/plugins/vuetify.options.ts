import { primaryColor } from '~/constants';

export default {
  theme: {
    themes: {
      dark: {
        primary: primaryColor,
      },
      light: {
        primary: primaryColor,
      },
    },
  },
};
