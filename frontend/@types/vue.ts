import type { PropOptions as VuePropOptions } from 'vue';
import type { MetaInfo as VueMetaInfo } from 'vue-meta';

export type MetaInfo = VueMetaInfo;
export type PropOptions<T> = VuePropOptions<T>;
