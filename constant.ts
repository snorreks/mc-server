export const projectId = ''; // Project ID

export const vmZone = 'europe-west1-b';

export const vmInstance = 'mc-server';

export const ipAddress = ''; // IP address of the VM instance

export const hasMap = true;

export const mapIpAddress = '8100';

export const firebasePublicConfig = {
  apiKey: '',
  authDomain: `${projectId}.firebaseapp.com`,
  projectId: `${projectId}`,
  storageBucket: `${projectId}.appspot.com`,
  messagingSenderId: '',
  appId: '',
  measurementId: '',
};
