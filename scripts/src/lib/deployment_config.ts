// scripts/src/lib/deployment_config.ts
// Re-exports all shared config from the root config.ts.

export {
  MC_DIFFICULTY,
  MC_MAX_PLAYERS,
  MC_MEMORY,
  MC_VIEW_DISTANCE,
  PROJECT_ID,
  STATIC_IP_NAME,
  VM_HAS_MAP,
  VM_INSTANCE,
  VM_IP,
  VM_MAP_PORT,
  VM_ZONE,
} from '../../../config';
